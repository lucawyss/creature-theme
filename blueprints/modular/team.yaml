title: Features
@extends: default

form:
  fields:
    tabs:
      fields:
        advanced:
          fields:
            columns:
              fields:
                column1:
                  fields:
                    name:
                      default: modular/features
                      @data-options: '\Grav\Common\Page\Pages::modularTypes'
            overrides:
              fields:
                header.template:
                  default: modular/features
                  @data-options: '\Grav\Common\Page\Pages::modularTypes'
        features:
          type: tab
          title: Members
          fields:
            header.members:
              name: members
              type: list
              label: Members

              fields:
                .name:
                  type: text
                  label: Name
                .image:
                  type: text
                  label: Photo
                .bio:
                  type: textarea
                  label: Biographie

{% extends 'partials/base.html.twig' %}

{% block content %}

    {{ content }}

    <div class="alert">{{ form.message }}</div>
    <p>{{ 'CREATURE.EMAIL_SUMMARY' |t }}</p>

    {% include "forms/data.html.twig" %}

{% endblock %}
